This is an implemtation of a Binary Search Tree that can store any arbitrary struct in its nodes. 

You can modify the struct definition in structs.h, and the compare fuction in compare.c, to accomidate different struct types and comparisons of relevant values within those structs.

To run:
Copy the files to your directory and run
$ make all
$ prog

